/**
 * @context-engine/server
 * Community Edition Backend Server
 */

import { hello } from '@context-engine/core';

console.log('🚀 Context Engine Server');
console.log(hello());
console.log('Server is ready!');

